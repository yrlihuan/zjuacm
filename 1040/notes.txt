一道麻烦的匪夷所思的题目。

通过将描述转化为有向图，可以很容易找到从出发地到目的地的路径。

难点一个是有向图的转化，直接将题中的Point和Signal转换为图上的点会有问题。
实际中，将一个Point转化为6个点，而Signal则和特定的点进行绑定。
有了基本思路，写起来还是很麻烦。输入是一种十分不友好的图的描述方式。
另外里面的交通规则实现起来着实困难。最终的实现中，先找路径，然后根据规则
将特定的不符合规则的路径进行调整。
